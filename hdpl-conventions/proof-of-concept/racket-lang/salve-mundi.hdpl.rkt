#lang hdpl/linguam/lat-Latn

;; This file will inject variable 'data-lat-Latn' on console with all raw content of itself
;; So run this data on console (or require in another script) and then just execute
;;    data-lat-Latn

(require hdp-toolchain)

(print hello-world)